diff --git a/chemglam/model/chemglam.py b/chemglam/model/chemglam.py
--- a/chemglam/model/chemglam.py
+++ b/chemglam/model/chemglam.py
@@ -264 +264,4 @@ class ChemGLaM(L.LightningModule):
-        return self(batch)
+        output, weight = self(batch)
+        if self.config.save_attention_weight:
+            return output, weight
+        return output
diff --git a/predict.py b/predict.py
--- a/predict.py
+++ b/predict.py
@@ -3,0 +4 @@ import json
+from pathlib import Path
@@ -45 +46,4 @@ def main():
-    if config.target_columns is not None:
+    if (
+        config.target_columns is not None
+        and config.to_dict().get("run_test_before_predict", True)
+    ):
@@ -52 +55,0 @@ def main():
-        predictions.append(res[0])
@@ -54 +57,2 @@ def main():
-            weights = res[1]
+            prediction, weights = res
+            predictions.append(prediction)
@@ -56,0 +61,2 @@ def main():
+        else:
+            predictions.append(res)
@@ -86 +92,5 @@ def main():
-    df_pred.to_csv(f"./logs/{config.experiment_name}/prediction.csv", index=False)
+    prediction_path = Path(f"./logs/{config.experiment_name}/prediction.csv")
+    prediction_path.parent.mkdir(parents=True, exist_ok=True)
+    temporary_path = prediction_path.with_suffix(".csv.tmp")
+    df_pred.to_csv(temporary_path, index=False)
+    temporary_path.replace(prediction_path)
